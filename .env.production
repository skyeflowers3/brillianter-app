# Production-only: route AI requests through the deployed aiProxy Cloud Function (OpenAI).
# Local `npm run dev` (development mode) ignores this file and stays on the free mock provider.
VITE_AI_PROVIDER=cloud
